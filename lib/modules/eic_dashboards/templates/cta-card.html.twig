<a class="cta-card {{ variant ? 'cta-card--' ~ variant }}" href="{{ link }}">
  <div class="cta-card__icon">
    <img src="{{ path ~ icon }}.svg" alt="{{ icon }}">
  </div>
  <div class="cta-card__title">{{ title }}</div>
</a>
