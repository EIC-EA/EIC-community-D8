# Schema for the configuration files of the EIC Projects module.
eic_projects.settings:
  type: config_object
  label: 'EIC Projects settings'
  mapping:
    api_key:
      type: string
      label: 'CORDIS Data Extraction API key'
