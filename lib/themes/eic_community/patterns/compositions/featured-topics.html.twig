{% set title_element = title_element|default('h3') %}

{% if items is not empty %}
  <div class="ecl-featured-list ecl-featured-list--topics">
    {% if title is not empty %}
      {% if title is not empty %}
        <{{ title_element }} class="ecl-featured-list__title">{{ title }}</{{ title_element }}>
      {% endif %}
    {% endif %}

    <div class="ecl-featured-list__items">
      {% for item in items %}
        <div class="ecl-featured-list__item">
          {% include '@ecl-twig/ec-component-tag/ecl-tag.html.twig' with {
            extra_classes: 'ecl-featured-list__item-tag',
            tag: {
              type: 'link',
              path: item.path,
              label: item.label
            }
          } only %}
        </div>
      {% endfor %}
    </div>
  </div>
{% endif %}
