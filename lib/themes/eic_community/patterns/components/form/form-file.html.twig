{% embed "@theme/patterns/components/well.html.twig" with field|default({})|merge({
  extra_classes: 'ecl-well--is-blue',
}) only %}
  {% block content %}
    {% include '@ecl/file-upload/file-upload' with field|default({})|without('type')|merge({
      width: 'full',
    }) %}
  {% endblock %}
{% endembed %}
