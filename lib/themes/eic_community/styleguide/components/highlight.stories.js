import HighlightTemplate from '@theme/patterns/components/highlight.html.twig';

import highlight from '@theme/data/highlight.data';
import common from '@theme/data/common.data';

export const Base = () => HighlightTemplate(highlight);

export default {
  title: 'Components / Highlight',
};
