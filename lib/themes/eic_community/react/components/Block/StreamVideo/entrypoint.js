import React from 'react';
import ReactDOM from 'react-dom';
import StreamVideo from './index';

const element = document.getElementById('react-stream-video-block');

ReactDOM.render(<StreamVideo />, element);
